# Shared PaoPaoDNS data directory
DATA_DIR=/data

# Required unless WEB_UI_ALLOW_NO_AUTH=true
WEB_UI_TOKEN=replace-with-a-long-random-token
WEB_UI_ALLOW_NO_AUTH=false

# Optional comma-separated reverse proxy IPs or CIDRs allowed to set X-Forwarded-For
WEB_UI_TRUSTED_PROXIES=
